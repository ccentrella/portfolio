<% if @message.errors.any? %>
  <div>
    <div class = "flash flash-error">
      <p>The form contains <%= pluralize(@message.errors.count, "error") %>.</p>
    </div>
      <ul>
        <% @message.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>
