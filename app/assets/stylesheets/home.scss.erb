.hero {
    display: block;
    align-items: center;

    .logo {
        font-size: 3.2em;
        margin: 0;
        padding: 0.25em;
    }

    .logo-tagline {
        padding: 0 0.5em 0 0.75em
    }
}

.hero-image {
    display: flex;
    order: 2;
    flex: 1 0 0;
    align-items: center;
    height: calc(var(--vh, 1vh) * 100 - 9px);
    padding-bottom: 20px;
    background-color: #3D5D212e;
    background-repeat: no-repeat;
    background-size: cover;
    background-image: url(<%= asset_path 'background-home.png' %>);
    border-bottom: 10px solid #3c4006c7;
}

/* .hero-overlay {
    margin-top: 150px;
    padding-bottom: 50px;
    width: 100%;
    background-color: #3D5D2123;
} */

.hero-about {
    padding: 3em 2em 0 2.25em;
    order: 1;
    flex: 1 0 0;
}

@media screen and (min-width:750px) {
    .hero {
        display: flex;
        background-color: black;
        margin-bottom: -5em;
        border-bottom: 10px solid #3B4D0A;

        p, a, h1 {
            color: whitesmoke;
        }

        main {
            margin-top: 100px;
        }

        .hero-image {
            padding: 0 2em;
            border: none;
        }

        .hero-overlay {
            margin-top: 50px;
            background: transparent;
        }

        .hero-about {
            padding: 0 2em;
        }
    }
}